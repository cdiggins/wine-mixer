using WineMixer;

namespace WineMixerTests;

public static class TransitionTests
{
    public static Transition BestTransition(State state, Transition transition)
    {
        // ...
        // How do I score everything? 

        // Well the first thing is, add the wine that is the most missing 
        // from the best mix. 
        
        // What wine should I add at each step? 
        // 

        throw new NotImplementedException();
    }
}